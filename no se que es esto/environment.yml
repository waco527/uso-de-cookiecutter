# conda env create --file environment.yml
name: no se que es esto
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - fs
  - jupyter
  - jupyterlab
  - pathlib
  - pip
  - pyprojroot
  - python=3.9
  - pip:
    - pyhere
    